## 1. Хамгийн үнэ цэнэтэй assertion аль байсан бэ? Яагаад?

Хамгийн үнэ цэнэтэй тест нь **"Content-Type is JSON"** гэсэн header шалгалт байсан.
Учир нь энэ тест надад маш чухал зүйл зааж өгсөн: `pm.response.to.have.header("Content-Type", /json/)` гэж бичихэд Postman дотор ажиллаж байсан ч Newman дээр fail болсон. Шалтгаан нь серверийн буцаасан утга `application/json; charset=utf-8` байсан бол миний regex `/json/` нь хангалтгүй тохирохгүй байсан юм. Энэ нь "дэлгэц дээр ажиллаж байна" гэдэг нь "бодит орчинд ажиллана" гэсэн үг биш гэдгийг тодорхой харуулсан. Засвар нь `pm.expect(...).to.include("application/json")` болгох байсан бөгөөд энэ нь илүү уян хатан, тогтвортой assertion юм.

## 2. Negative test-ийн дэлгэрэнгүй тайлбар

`GET /posts/999999` гэсэн endpoint рүү байхгүй ID явуулснаар **404 Not Found** буцаахыг шалгасан. Энэ тест нь "систем буруу оролтод хэрхэн хариу өгч байна вэ?" гэдгийг баталгаажуулж байна. Зөвхөн хэвийн ажиллагааг шалгах нь хангалтгүй — хэрэглэгч буруу ID оруулах, сүлжээ тасрах, эвдэрсэн payload явуулах зэрэг "алдааны зам" бас API-ийн гэрээний нэг хэсэг юм. Хэрэв систем 404-ийн оронд 500 буцаавал өөрийн кодонд аль нэг хяналтгүй алдаа байна гэсэн үг — negative тест үүнийг урьдчилан илрүүлдэг.

## 3. Postman дотор ажиллаж байсан ч Newman дээр fail болсон уу?

Тийм, хоёр алдаа гарсан.

**Алдаа 1 — baseUrl хоосон:** Newman-д `env.dev.json` файлыг `-e` флагаар холбохоо мартахад `Invalid URI "http:///posts"` гэсэн алдаа гарсан. Postman дотор environment идэвхтэй байдаг ч Newman-д заавал тусгайлан дамжуулах хэрэгтэй. Засвар: команд дээр `-e postman/env.dev.json` нэмэх.

**Алдаа 2 — Content-Type assertion:** Дээр дурдсанчлан regex `/json/` Newman дотор хатуу тохирохгүй байсан. Постман уян хатан байдаг ч Newman нарийн шаардлага тавьдаг.

## 4. Token болон Secret-ийг хэрхэн зохицуулсан вэ?

JSONPlaceholder auth шаардахгүй тул энэ лабораторт бодит token байгаагүй. Гэхдээ `env.dev.json` болон `env.ci.json` файлуудад `baseUrl` болон бусад хувьсагчдыг environment level-д хадгалж, collection.json дотор `{{baseUrl}}` гэж лавлаж ашиглав. Хэрэв бодит token байсан бол `env.dev.json`-д `REPLACE_WITH_REAL_TOKEN` гэж placeholder тавьж, `.gitignore`-д оруулах байсан. GitHub Actions-д бол `${{ secrets.API_TOKEN }}` ашиглан GitHub Secrets-ээс авах байсан. Гол дүрэм: **жинхэнэ credential-ийг хэзээ ч git-д commit хийж болохгүй.**

## 5. API өөрчлөгдвөл аль хэсэг хамгийн их эвдрэх вэ?

Хамгийн эмзэг хэсэг нь **response-ийн schema шалгадаг тестүүд** юм. Жишээ нь `pm.expect(data[0]).to.have.property("email")` гэсэн тест `/posts/1/comments`-ийн response дотор `email` талбар заавал байна гэж таамаглаж байна. Хэрэв API шинэчлэгдэж талбарын нэр өөрчлөгдвөл (жишээ нь `email` → `emailAddress`) энэ тест тэр даруй fail болно. Мөн request chaining маш эмзэг — `firstPostId` хувьсагч эхний хүсэлтийн `response[0].id`-ээс хамаардаг тул тэр өгөгдлийн бүтэц өөрчлөгдвөл бүх chain эвдрэнэ. Үүнийг бууруулах арга: schema validation-д нарийн утга биш, **өгөгдлийн төрөл болон талбарын оршин байдлыг** шалгах; response бүтцийн өөрчлөлтийг эрт илрүүлэхийн тулд тестийн тоог нэмэгдүүлэх.